#include <stdio.h>
#include <wchar.h>

int32_t main () {
    do {
        eax = "Enter password: ";
        printf (eax);
        eax = 0x80485e1;
        isoc99_scanf (eax, edx, local_1ch);
        eax = local_1ch;
        /* pass.1964 */
        eax = wcscmp ("w0wgreat", eax);
        if (eax == 0) {
            puts ("Congrats!");
            eax = 0;
            return eax;
        }
        puts ("Wrong!");
    } while (1);
}
