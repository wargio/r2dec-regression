#include <stdio.h>
#include <wchar.h>

void main () {
    do {
        eax = "Enter password: ";
        printf (eax);
        eax = 0x80485e1;
        edx = local_1ch;
        *((int32_t*) local_4h) = edx;
        isoc99_scanf (eax);
        eax = local_1ch;
        *((int32_t*) local_4h) = eax;
        /* pass.1964 */
        wcscmp ("w0wgreat");
        if (eax == 0) {
            puts ("Congrats!");
            eax = 0;
            return eax;
        }
        puts ("Wrong!");
    } while (1);
}
